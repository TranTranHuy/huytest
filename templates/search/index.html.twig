{# {% extends 'home.html.twig' %} #}
{# {% block title %}Search{% endblock %} #}

{# {% block body %} #}
 <!--Main layout-->
{# <main>
<div class="container" style="margin-top: 50px"> #}
  <!-- Navbar -->
{# <nav class="navbar navbar-expand-lg navbar-dark mt-3 mb-5 shadow p-2" 
     style="background-color: #607D8B"
> #}
  <!-- Container wrapper -->
  {# <div class="container-fluid"> #}

    <!-- Navbar brand -->
    {# <a class="navbar-brand" href="#">Categories:</a> #}
    {# <p class="navbar-brand" style="margin-top: 12px">Staff:</p> #}

    <!-- Toggle button -->
    {# <button 
       class="navbar-toggler" 
       type="button" 
       data-bs-toggle="collapse" 
       data-bs-target="#navbarSupportedContent2">
      <i class="fas fa-bars"></i>
    </button> #}

    <!-- Collapsible wrapper -->
    {# <div class="collapse navbar-collapse" id="navbarSupportedContent2">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0"> #}

        <!-- Link -->
        {# <li class="nav-item acitve">
          <a class="nav-link text-white" href="#">All</a>
        </li>
        <li class="nav-item acitve">
          <a class="nav-link text-white" href="#">Hard-Working</a>
        </li>
        

      </ul>

      <form class="w-auto py-1" style="max-width: 15rem" action="{{path('searchByName')}}">
        <input type="text" class="form-control rounded-0" placeholder="Search" aria-label="Search" name="name">
      </form>
    </div>
  </div> #}
  
  <!-- Container wrapper -->
{# </nav>

  <section>
  <div class="text-center">
    <div class="row">
      {% for s in search %}
      <div class="col-lg-3 col-md-6 mb-4">
        <div class="card">
          <div class="bg-image hover-zoom ripple ripple-surface ripple-surface-light"
            data-mdb-ripple-color="light">
            {% set img = s.image %}
            <img src="{{asset('uploads/'~img)}}" class="w-100" 
                 style="
                        height: 300px;
                 "
            />
          </div>
          <div class="card-body">
            <a href="{{path('staffRead',{"id":s.id})}}" class="text-reset">
              <style>h5:hover{
                              background-color: rgb(169, 226, 252);
                              border-radius: 10px;
              }</style>
              <h5 class="card-title mb-2" style="margin-left: 40px; width: 200px">{{s.name}}</h5>
            </a>
            <h6 class="mb-3 price">{{s.address}}</h6>
          </div>
        </div>
      </div>
      {% endfor %}
    </div>

   
  </div>
</section>
{% endblock %} #}
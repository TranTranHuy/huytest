{# {% extends 'base.html.twig' %}

{% block title %}Hello StaffController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ 'C:/Users/NGUYEN HOAI PHONG/ProjectTest/src/Controller/StaffController.php'|file_link(0) }}">src/Controller/StaffController.php</a></code></li>
        <li>Your template at <code><a href="{{ 'C:/Users/NGUYEN HOAI PHONG/ProjectTest/templates/staff/index.html.twig'|file_link(0) }}">templates/staff/index.html.twig</a></code></li>
    </ul>
</div>
{% endblock %} #}

{% extends 'admin.html.twig' %}

{% block title %}Ingredient!{% endblock %}

{% block body %}
  <div className="page-heading pb-2 mt-4 mb-2 ">
    <h3 style="text-align: center; background: yellow">Ingredient Manager</h3> 

    {# <form class="w-auto py-1" style="max-width: 15rem" action="{{path('searchByName')}}">
        <input type="search" class="form-control" style="border-radius: 10px" placeholder="Search" aria-label="Search" name="name">
      </form> #}

        <a href="{{path('ingredientAdd')}}">
            <button type="button" class="btn btn-outline-primary">Insert</button>
        </a>
        <a href="{{path('app_check')}}">
            <button type="button" class="btn btn-outline-primary">Check Low Quantity</button>
        </a>
        {# <a href="{{path('check_high_quantity')}}">
            <button type="button" class="btn btn-outline-primary">Check residual quantity</button>
        </a> #}
        <a href="{{path('ingredientPage')}}">
            <button type="button" class="btn btn-outline-primary">All</button>
        </a>
                </div>
                <div class="page-content">
                <div class="btn-group" role="group" aria-label="Basic outlined example">
               
                </div>
                <div class="card">
                    <div class="container mb-3">
                        <div class="table-responsive text-nowrap">
                      <table class="table table-striped">
                        <thead style="background:#FFCCCC">
                          <tr>
                            <th>Id</th>
                            <th>Ingredient Name</th>
                            <th>Quantity</th>
                            <th>Action</th>
                          </tr>
                        </thead>
                        <tbody class="table-border-bottom-0">
                          {% for i in ingredient %}
                              <tr>
                                  <td>{{ i.id}}</td>
                                  <td>{{ i.ingredientName}}</td>
                                  <td>{{ i.quantity}}</td>
                                  <td>
                                    <a href="{{path('ingredientEdit',{'id':i.id})}}">Edit</a>
                                  <a href="{{path('ingredientDelete',{'id':i.id})}}">Delete</a></td>
                              </tr>
                          {% endfor %}
                          
                        </tbody>
                      </table>
                    </div>
                </div>
                </div>
                </div>
{% endblock %}

